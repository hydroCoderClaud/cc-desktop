# v1.2.1 快速测试清单

## 🎯 核心功能（必测）

### 1. Commands 标签页 ⌨️

- [ ] 打开 Commands 标签页，能看到命令列表
- [ ] 点击任一命令，输入框自动填入命令（如 `/commit`）
- [ ] 搜索功能正常工作

### 2. Hooks 标签页 🪝

- [ ] 打开 Hooks 标签页，能看到 hooks 列表
- [ ] 点击任一 hook，打开编辑模态框
- [ ] 模态框样式正常（有清晰边框，保存按钮是蓝色）
- [ ] 修改 matcher、type、command 字段
- [ ] 点击保存，显示 "已保存" 提示
- [ ] **控制台无 "An object could not be cloned" 错误**

### 3. Tab Bar 布局

- [ ] 9 个标签全部可见，无溢出
- [ ] 标签顺序正确：💬📜🔧⌨️⚡🪝🌐🧩🤖

### 4. Bug 修复验证

- [ ] Hooks 面板不为空（至少显示一些 hooks）
- [ ] AI 助手：添加新 API 配置后，下拉列表立即更新

---

## 🔧 详细测试（推荐）

### Commands 标签页

- [ ] 分类默认折叠（▶ 图标）
- [ ] 点击分类展开/折叠（▶ ↔ ▼）
- [ ] 刷新按钮 (🔄) 正常工作
- [ ] 空状态显示正常（卸载所有插件后）

### Hooks 标签页

- [ ] Hook 编辑器使用 Naive UI 组件
- [ ] Event 字段只读（灰色背景）
- [ ] Type 下拉菜单显示 Command、Bash、Block
- [ ] Command 字段为多行文本框（4行）
- [ ] 取消按钮关闭模态框且不保存
- [ ] 搜索功能正常

### UI 一致性

- [ ] Hooks 编辑模态框与 Prompts 编辑模态框样式一致
- [ ] Skills 标签页默认折叠（与 Commands 一致）
- [ ] 所有搜索框使用统一的 ⌕ 图标

---

## ⚡ 快速验证（5分钟）

只测试最关键的功能：

1. **打开 Commands 标签页** → 能看到命令列表 → 点击命令插入到输入框
2. **打开 Hooks 标签页** → 点击任一 hook 编辑 → 修改并保存 → 无错误
3. **查看 Tab Bar** → 9 个标签全部可见
4. **AI 助手测试** → 添加新 API 配置 → 下拉列表立即更新

---

## 📋 测试环境检查

### 安装测试插件

```bash
claude plugin install plugin-dev
claude plugin install document-skills
claude plugin install superpowers
```

### 启动开发模式

```bash
npm run dev
```

### 打开开发者工具

- 按 F12 查看控制台错误

---

## 🐛 已知问题检查

### 关键 Bug 验证

1. **"An object could not be cloned" 错误** (已修复)
   - 编辑并保存 hook
   - 检查控制台无此错误

2. **Hooks 面板显示为空** (已修复)
   - 打开 Hooks 标签页
   - 能看到 hooks 列表
   - 控制台无 "getGlobalSettingsPath" 错误

3. **AI 配置列表不更新** (已修复)
   - 添加新 API 配置
   - 打开 AI 助手设置面板
   - 下拉列表包含新配置

---

## ✅ 通过标准

- [ ] 所有核心功能正常工作
- [ ] 无控制台错误或警告
- [ ] UI 样式统一一致
- [ ] 所有 Bug 已修复

---

## 📝 问题反馈模板

如发现问题，请提供：

```
【问题描述】
简要描述遇到的问题

【复现步骤】
1. 打开...
2. 点击...
3. 看到...

【预期结果】
应该显示/执行...

【实际结果】
实际显示/执行...

【环境信息】
- OS: Windows/macOS/Linux
- 版本: v1.2.1
- 控制台错误: [粘贴错误信息]

【截图】
[如果可能，附上截图]
```

---

**祝测试顺利！**
