# Claude Code Desktop

Claude Code 官方风格的独立桌面终端应用。

## 项目特点

- ✨ **完全独立** - 不依赖 Web 版代码，独立开发和维护
- 🎨 **优雅界面** - Claude 官方风格的现代化 UI 设计
- 🚀 **简单高效** - 本质是 Claude Code CLI 启动器 + 终端模拟器
- 💾 **本地存储** - 所有配置和项目列表保存在本地
- 🌓 **主题切换** - 支持浅色和深色主题
- 📁 **项目管理** - 快速切换最近打开的项目文件夹

## 快速开始

### 安装依赖

```bash
npm install
```

### 开发模式

```bash
npm run dev
```

### 打包应用

```bash
# Windows
npm run build:win

# macOS
npm run build:mac

# Linux
npm run build:linux
```

## 项目结构

```
cc-desktop/
├── src/
│   ├── main/                 # 主进程
│   │   ├── index.js          # Electron 入口
│   │   ├── config-manager.js # 配置管理
│   │   ├── terminal-manager.js # PTY 终端管理
│   │   └── ipc-handlers.js   # IPC 通信
│   │
│   ├── preload/              # 预加载脚本
│   │   └── preload.js        # IPC 安全桥接
│   │
│   └── renderer/             # 渲染进程
│       ├── index.html        # 主界面
│       └── js/
│           └── app.js        # 应用逻辑
│
├── assets/                   # 应用图标
├── docs/                     # 文档
└── package.json              # 项目配置
```

## 核心功能

### 项目管理
- 添加项目：选择文件夹，自动添加到最近列表
- 项目列表：显示最近打开的项目（最多10个）
- 快速切换：点击项目即可选择

### 终端功能
- 在选定目录启动 PowerShell/Bash
- 支持 xterm.js 终端特性（颜色、链接、调整大小）
- 自动传递 ANTHROPIC_API_KEY 环境变量

### 配置管理
- API Key 配置
- 主题切换（浅色/深色）
- 终端字体和字号设置
- 配置文件位置：`%APPDATA%/claude-code-desktop/config.json`

## 配置文件

位置：`%APPDATA%/claude-code-desktop/config.json`

```json
{
  "recentProjects": [
    {
      "id": "uuid",
      "name": "项目名称",
      "path": "C:\\workspace\\project",
      "lastOpened": "2026-01-12T10:30:00Z",
      "icon": "📁",
      "pinned": false
    }
  ],
  "settings": {
    "theme": "light",
    "claudeApiKey": "",
    "anthropicApiKey": "sk-ant-...",
    "terminal": {
      "fontSize": 14,
      "fontFamily": "Consolas, monospace"
    },
    "maxRecentProjects": 10
  }
}
```

## 使用说明

1. **启动应用**：运行打包后的应用或 `npm run dev`
2. **添加项目**：点击 "+ New session" 选择文件夹
3. **选择项目**：点击左侧项目列表中的项目
4. **连接终端**：点击 "Connect" 按钮
5. **使用终端**：在终端中输入 `claude code` 启动 Claude Code CLI

## 开发说明

### 技术栈
- **Electron** - 桌面应用框架
- **node-pty** - PTY 终端管理
- **xterm.js** - 终端 UI
- **Node.js** - 主进程运行时

### 架构设计
- **单用户模式** - 无需认证和用户管理
- **简单数据模型** - 单个 JSON 配置文件
- **直接进程管理** - 一个项目 = 一个 PTY 进程
- **IPC 通信** - 渲染进程和主进程通过 IPC 交互

### 与 Web 版的区别
- ❌ 无用户认证系统
- ❌ 无会话管理（30分钟超时等）
- ❌ 无 WebSocket 通信
- ❌ 无模板/Prompt 三级管理
- ✅ 更简单的项目管理
- ✅ 更直接的进程生命周期
- ✅ 更快的启动速度

## 许可证

MIT License

## 作者

HydroCoder Team
